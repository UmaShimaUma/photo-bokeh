# 開発エージェント向け指示

このリポジトリで作業するエージェントは、以下のルールを必ず守ってください。

## 言語とコミュニケーション

- Issue、PR本文、PRタイトル、コミットメッセージ、レビューコメント、設計メモは日本語で書くこと。
- ユーザー向けの説明も、特別な指定がない限り日本語で書くこと。
- 仕様の判断に迷う場合は、正確な物理計算よりも「写真としての見え方が直感的にわかること」を優先すること。
- このリポジトリに関係しないドメイン情報、地名、変数名、例示を混ぜないこと。背景ボケシミュレーターと無関係な内容は記載しないこと。

## 仕様追加・変更時の必須作業

- 追加仕様や変更仕様が出た場合は、必ず `testcase.md` に日本語のテストケースを追加・更新すること。
- `testcase.md` を更新した後、対応する自動テストコードも追加・更新すること。
- テストケースと自動テストの内容が一致していることを確認すること。
- テストが実行できない環境制約がある場合は、理由と代替確認方法を記録すること。

## 実装方針

- MVPでは、背景画像レイヤーと被写体レイヤーを分離し、背景に CSS blur 相当のぼかし量を適用する表現を優先すること。
- カメラ設定の計算は、初心者が理解しやすい表示を優先し、過度に厳密な光学計算に寄せすぎないこと。
- 入力値は `CameraSettings`、計算結果は `SimulationResult` のような明確なデータ構造にまとめること。
- UIはスマホ・PC両対応を前提にし、スライダー操作でリアルタイムに写真風プレビューが変化する体験を重視すること。
- アプリ実装は TypeScript と Next.js を前提にし、Vercel へデプロイしやすい構成を維持すること。

## テスト方針

- テスト名・テストケース説明は日本語で書くこと。
- ボケ量スコア、ボケ量ラベル、被写界深度、入力値の境界について優先的にテストすること。
- 仕様追加時は「日本語のテストケース」→「自動テストコード」の順に更新すること。

## Git / GitHub

- コミットメッセージは日本語で書くこと。
- Pull Request のタイトルと本文は日本語で書くこと。
- PR本文には、変更内容、テスト結果、未対応事項があれば日本語で記載すること。
