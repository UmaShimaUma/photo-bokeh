# テストケース

このドキュメントは、背景ボケシミュレーターの仕様を日本語で確認するためのテストケース一覧です。仕様追加時は、先にこのファイルへテストケースを追記し、その後に対応する自動テストコードを追加します。

## TC-001: 初期設定で背景がしっかりボケる

- **前提**: フルサイズ、50mm、F2.8、被写体距離2m、背景距離5mを指定する。
- **操作**: シミュレーション計算を実行する。
- **期待結果**:
  - ボケ量スコアが 51〜75 の範囲に入る。
  - ボケ量ラベルが「しっかりボケる」になる。
  - 被写界深度の手前、奥、範囲が正の値として返る。

## TC-002: F値を小さくすると背景ボケが大きくなる

- **前提**: 焦点距離、被写体距離、背景距離、センサーサイズは同じにする。
- **操作**: F1.4 と F8 のボケ量スコアを比較する。
- **期待結果**:
  - F1.4 のボケ量スコアが F8 より大きい。

## TC-003: 焦点距離を長くすると背景ボケが大きくなる

- **前提**: F値、被写体距離、背景距離、センサーサイズは同じにする。
- **操作**: 28mm と 85mm のボケ量スコアを比較する。
- **期待結果**:
  - 85mm のボケ量スコアが 28mm より大きい。

## TC-004: 被写体に近づくと背景ボケが大きくなる

- **前提**: F値、焦点距離、背景距離、センサーサイズは同じにする。
- **操作**: 被写体距離1m と 3m のボケ量スコアを比較する。
- **期待結果**:
  - 被写体距離1m のボケ量スコアが 3m より大きい。

## TC-005: 背景が遠いほど背景ボケが大きくなる

- **前提**: F値、焦点距離、被写体距離、センサーサイズは同じにする。
- **操作**: 背景距離1m と 10m のボケ量スコアを比較する。
- **期待結果**:
  - 背景距離10m のボケ量スコアが 1m より大きい。

## TC-006: ボケ量スコアから日本語ラベルが決まる

- **前提**: ボケ量スコアの境界値を確認する。
- **操作**: 0、20、21、50、51、75、76、100 のラベルを取得する。
- **期待結果**:
  - 0〜20 は「ほぼボケない」。
  - 21〜50 は「少しボケる」。
  - 51〜75 は「しっかりボケる」。
  - 76〜100 は「大きくボケる」。

## TC-007: 入力値が範囲外の場合はMVPの許容範囲に丸める

- **前提**: F値、焦点距離、被写体距離、背景距離に範囲外の値を指定する。
- **操作**: シミュレーション計算を実行する。
- **期待結果**:
  - 焦点距離は 20〜300mm に丸められる。
  - F値は F1.4〜F16 に丸められる。
  - 被写体距離は 0.5〜10m に丸められる。
  - 背景距離は 1〜30m に丸められる。

## TC-008: AGENTS.md にアプリと無関係な情報を含めない

- **前提**: 開発エージェント向け指示を確認する。
- **操作**: `AGENTS.md` の内容を検査する。
- **期待結果**:
  - `pic_location` のような、このアプリと無関係な変数名を含まない。
  - `淡路島` のような、このアプリと無関係な地名を含まない。
  - 無関係な内容を記載しないための注意書きが存在する。

## TC-009: Next.js と TypeScript でVercel向けに起動できる構成にする

- **前提**: アプリをVercelへデプロイする想定で構成ファイルを確認する。
- **操作**: `package.json`、`tsconfig.json`、`next.config.ts`、`app/page.tsx` を検査する。
- **期待結果**:
  - `package.json` に `next`、`react`、`react-dom`、`typescript` が含まれる。
  - `package.json` に `dev`、`build`、`start`、`typecheck`、`test` スクリプトが含まれる。
  - `tsconfig.json` が存在し、TypeScriptの厳格な型検査が有効になっている。
  - `app/page.tsx` が存在し、シミュレーションUIを TypeScript / React で実装している。
